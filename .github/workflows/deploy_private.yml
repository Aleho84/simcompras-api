name: deploy_private
on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - name: Create .env file
        run: |
          touch .env
          echo NODE_ENV=production >> .env
          echo RUN_MODE=fork >> .env
          echo ENV_PROTOCOL=http >> .env
          echo ENV_HOST=${{ secrets.HOST }} >> .env
          echo ENV_PORT=${{ secrets.PORT }} >> .env
          echo ENV_SECRET_STRING=${{ secrets.SECRET_STRING }} >> .env
          echo ENV_TIME_SESSION=60 >> .env
          echo ENV_DB_MODE=mongoDB >> .env
          echo ENV_MONGOOSE_URI=${{ secrets.MONGOOSE_URI }} >> .env
          cat .env
      - run: npm run build --if-present
      - run: npm run test --if-present
